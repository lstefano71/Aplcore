<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>preview</LangVersion>

    <!-- AOT Publishing -->
    <PublishAot>true</PublishAot>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>

    <!-- Assembly metadata -->
    <RootNamespace>AplcoreHandler</RootNamespace>
    <AssemblyName>AplcoreHandler</AssemblyName>

    <!-- MinVer: git tag v{major}.{minor}.{patch} is the single source of truth -->
    <MinVerTagPrefix>v</MinVerTagPrefix>
    <!-- MinVer already encodes commit info in pre-release labels; suppress SDK duplication -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DotNet.Glob" Version="3.1.3" />
    <PackageReference Include="Spectre.Console" Version="0.55.2" />
    <PackageReference Include="MinVer" Version="6.*" PrivateAssets="all" />
  </ItemGroup>

</Project>
