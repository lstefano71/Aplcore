## Agent skills

### Issue tracker

GitHub Issues in `lstefano71/Aplcore` (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Using canonical triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (root `CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.
